import java.util.*;
class HelloWorld {
    public static void main(String[] args) {
        String s = "john doe";
        char a[] = s.toCharArray();
        Arrays.sort(a);
        for(int i=0; i < 7 ; i++){
            if(a[i]==a[i+1]){
                System.out.println(a[i]);
            }
        }
    }
}
